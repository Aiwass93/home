#!/bin/sh
# Force CPU rendering (Prevent Crash)
export LIBGL_ALWAYS_SOFTWARE=1

# Reset other flags that might cause glitches
unset GSK_RENDERER
unset WEBKIT_DISABLE_COMPOSITING_MODE
unset WEBKIT_DISABLE_DMABUF_RENDERER

# Launch
exec /usr/bin/foliate "$@" 2> /dev/null
