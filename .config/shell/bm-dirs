# You can add comments to these files with #
# I could never understand why people use folders with capitalized characters in home directory #
cac ${XDG_CACHE_HOME:-$HOME/.cache}
cf  ${XDG_CONFIG_HOME:-$HOME/.config}
d   ${XDG_DOWNLOAD_DIR:-$HOME/misc/dl}
dt  ${XDG_DATA_HOME:-$HOME/.local/share}
rr  $HOME/.local/src
h   $HOME
m   ${XDG_MUSIC_DIR:-$HOME/media/music}
mn  /mnt
pp  ${XDG_PICTURES_DIR:-$HOME/media/pics}
sc  $HOME/.local/bin
src $HOME/.local/src
vv  ${XDG_VIDEOS_DIR:-$HOME/media/vids}
